# 🐧 Курс консоли Linux — VK Mini App

Интерактивное приложение для обучения работе с консолью Linux. Состоит из 10 видеоуроков, встроенных через виджет VK Video. Данные пользователя загружаются через VK Bridge, а контент курса подгружается динамически из JSON-файлов.

## 📌 Функциональность

- Список из 10 шагов (уроков) с номерами и названиями, загружаемый из `course.json`.
- При клике на шаг открывается встроенное видео с автозапуском (данные видео берутся из отдельного JSON-файла урока).
- Видео встраивается через официальный виджет `vk.com/video_ext.php`.
- Кнопка «Назад к урокам» для возврата к списку.
- Отображение аватара и имени текущего пользователя VK (если запущено в VK Mini App).
- Встроенная отладка (лог-панель) для разработчиков.
- Интеграция с Яндекс.Метрикой для отслеживания событий.

## 🛠 Технологии

- Чистый HTML, CSS и JavaScript (без фреймворков).
- VK Bridge (для работы внутри VK Mini App).
- Яндекс.Метрика (счётчик `110303584`).
- Данные курса хранятся в JSON-файлах, что упрощает добавление новых уроков и курсов.

## 🚀 Установка и запуск

Проект состоит из нескольких статических файлов. Для запуска достаточно:

1. Скачать или склонировать репозиторий.
2. Открыть файл `index.html` в браузере.

```bash
git clone <url-репозитория>
cd <папка-проекта>
# Открыть index.html в браузере
```

> **Важно:** все файлы должны быть доступны по относительным путям. При локальном запуске может потребоваться запуск через локальный сервер (например, `python -m http.server`), так как `fetch` может не работать с протоколом `file://`.

## ⚙️ Настройка для VK Mini App

Если вы планируете использовать приложение как VK Mini App:

1. Зарегистрируйте приложение в [VK Admin](https://vk.com/apps?act=manage).
2. В настройках приложения укажите **URL** для запуска (например, `https://ваш-хост/index.html`).
3. Убедитесь, что в коде указан корректный `owner_id` группы, где размещены видео (в коде используется `-193665099` — замените на свой, если нужно).
4. Для работы VK Bridge необходимо, чтобы приложение запускалось внутри официального клиента VK (мобильного или веб-версии VK Mini Apps).

## 📁 Структура проекта

```
project/
├── index.html          # Главная страница (разметка)
├── style.css           # Все стили
├── app.js              # Движок приложения (загрузка, рендеринг, VK Bridge)
└── data/               # Данные курсов
    └── linux-console/  # Папка текущего курса (идентификатор)
        ├── course.json # Метаданные курса + список уроков
        └── lessons/    # Подпапка с уроками
            ├── 1.json
            ├── 2.json
            ├── ...
            └── 10.json
```

Каждый файл урока содержит `id`, `title`, `videoId`, `hash` для виджета, а также заготовки для `articles` и `tasks` (будущее расширение).

## 📖 Использование

1. **Главный экран** — список из 10 уроков с эмодзи, загруженный из `course.json`.
2. **Выбор урока** — клик по любой строке загружает детали урока из JSON и открывает видео.
3. **Просмотр видео** — видео запускается автоматически. Можно переключаться на полноэкранный режим.
4. **Возврат** — кнопка «Назад к урокам» скрывает видео и показывает список.
5. **Отладка** — клик по тексту версии (внизу) открывает панель с логами.

## 🐞 Отладка

- Нажмите на текст **«Версия 2.2.0»** внизу страницы — появится панель с логами.
- Также можно добавить параметр `?debug=true` в URL, чтобы панель была видна сразу.
- Все логи дублируются в консоль браузера.

## 📊 Метрика

В приложение встроен счётчик Яндекс.Метрики (ID `110303584`). При клике на каждый урок отправляется цель `step1` … `step10`. Вы можете заменить ID на свой или удалить блок с метрикой, если он не нужен.

## 📝 Примечания

- Видео доступны только при подключении к интернету (встраиваются через iframe).
- Для корректной работы внутри VK требуется установленное приложение VK или браузер с поддержкой VK Bridge.
- Весь код написан с учётом мобильных устройств (адаптивная вёрстка).

## 📄 Лицензия

Распространяется под лицензией **MIT**. Используйте и модифицируйте свободно.